import { SkillStatus } from "../../types/concert_types";
import { WapSkillLevel } from "../../types/wap/skill_waps";
import { Concert } from "../concert";

export default function performSkill(
  this: Concert,
  skill: WapSkillLevel,
  skillStat: SkillStatus,
  isCharaSkill: boolean,
  Parti
) {

}