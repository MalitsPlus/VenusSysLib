import { CardParameter, CardRarity } from "../../types/proto/proto_master";
import { WapCard } from "../../types/wap/card_waps";

export const getCard = (
  id: string
): WapCard | undefined => {
}

export const getCardParam = (
  id: string,
  level: number
): CardParameter {
}

export const getCardRarity = (
  rarity: number
): CardRarity {
}
